<div>
  <div  id="mySearchDropdown" class="dropdown-content background-color">
    <form action="/offer/offer-search">
      <label for="search-field"></label>
      <input
        type="text"
        name="searchfield"
        id=""
        class="nav-btn-actv"
        {{#if searchTerm}}value="{{searchTerm}}"{{/if}}
        placeholder={{#if searchTerm}}{{searchTerm}}{{else}}"Type in anything …"{{/if}}
      />
      <button formaction="/offer/offer-search?sort=date" class="go-button nav-btn">GO!</button>
    </form>
  </div>
   {{#if filteredOffers}}
      <div style="position: static" class="search-sorting-button-div einzug-links">
        {{#if searchTerm}}
        <button formaction="/offer/offer-search?sort=price" class="button-sorting nav-btn ">S€</button>
        <button formaction="/offer/offer-search?sort=price-descending" class="button-sorting nav-btn" >S€€€</button>
        <button formaction="/offer/offer-search?sort=date" class="button-sorting nav-btn" >SNewest</button>
        <button formaction="/offer/offer-search?sort=date-oldest" class="button-sorting nav-btn" >SOldest</button>
        {{/if}}

      </div>
      {{/if}}
</div>
