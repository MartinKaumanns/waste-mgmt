 {{>navbar}}
<div id="profile-head">
    <div>
      <img src="{{profile.picture}}" alt="Profile Picture" />
    </div> 
    <div class="profile-details">
      <div>
        <h3>Hi, I’m {{profile.name}}</h3>
        {{!-- <div><strong>Name</strong>: {{profile.name}}</div> --}}
          <div>Im am working in the fields of {{#each profile.genres}}{{this}}{{#unless @last}} and {{/unless}} {{/each}}</div>
        
          <div>Right now I am based in Berlin,  {{profile.location}}</div>
        <div>If you want to contact me, shoot me a mail: {{profile.email}}</div>
    </div>
    </div>
 
{{#if userIsOwner}}
<form action="/user/{{profile._id}}/edit" >
  <button class="profile-btn">Edit profile</button>
</form>

<form action="/user/{{ profile._id}}/delete" method="POST">
  <button class="erase-btn">Delete profile</button>
</form>
{{/if}}
</div>


<h1>{{ profile.name }}’s Offers:</h1>
<div class="offersWrapper">

{{#if userOffers}}
{{#each userOffers}}
  {{>offersCard}}
{{#unless @last}}<br />{{/unless}}
{{/each}}
{{else}}
<p>No active offers</p>
{{/if}}
</div>