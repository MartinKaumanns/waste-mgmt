<img src="{{offer.picture}}" alt="photo of object" />

<h1>
  {{offer.title}}
</h1>
<p>{{offer.creator.name}} | {{offer.createdAt}}</p>

<span><strong>Genres</strong>: {{#each offer.genres}}  {{this}} {{#unless @last}},{{/unless}}</span>
{{/each}}
<br />
<span><strong>Material</strong>: {{#each offer.material}}  {{this}} {{#unless @last}},{{/unless}}</span>
{{/each}}
<br />
<p>{{offer.description}}</p>
<span>
 <strong>Location</strong>: {{offer.location}}
</span>

{{#if offer.price}}
<h3>Price: {{offer.price}} €</h3>

{{else}}
<h3>{{offer.alternativepayment}}</h3>
{{/if}}

<form action="/offer/{{offer._id}}/edit" >
<button>Edit</button>
</form>